import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      // 'i.postimg.cc',
      "res.cloudinary.com"], // Reemplaza 'example.com' con el dominio de tus imágenes
},
};

export default nextConfig;
