export type PresentationColumns = {
    id: string,
    name: string,
    description: string,
    categoryId: string,
    categoryName: string,
    isAvailable: boolean,
    createdAt: string,
    updatedAt: string,
}
